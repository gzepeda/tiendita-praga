<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="TienditaDePraga.NewMesPage"
		Title="Nuevo Mes">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save" Clicked="Save_Clicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout Spacing="20" Padding="15">
			<Label Text="Nombre" FontSize="Medium" />
			<Entry Text="{Binding Item.Nombre}" FontSize="Small" />
			<Label Text="Año" FontSize="Medium" />
			<Editor Text="{Binding Item.Anio}" FontSize="Small" Margin="0" />
            <Label Text="Numero de Mes" FontSize="Medium" />
            <Editor Text="{Binding Item.NumeroMes}" FontSize="Small" Margin="0" />
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
